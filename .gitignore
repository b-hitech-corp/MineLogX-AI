.idea/

build/

# === Local session plan (never commit) ===
PLAN-ARCHITECTURE-GOAL.md

# === Secrets / credentials (never commit) ===
.env
.env.*
!.env.example
*.pem
.aws/

# === AWS discovery dumps (contain account IDs / ARNs) ===
**/infrastructure/discovery/

# === Terraform ===
**/.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
**/terraform.tfvars
**/*.auto.tfvars
# scratch config produced by `terraform plan -generate-config-out`
**/generated*.tf
# Lambda deployment zips built by the archive_file data source
**/modules/lambda/.build/
**/modules/lambda_layer/.build/
# Lambda layer python/ trees built by `fab lambda.build-layer` (pip installs)
**/backend/.layers/
onprem-aws/.lambda-layers/

# === CloudFormation ===
**/cloudformation/params/*.local.json
packaged-*.yaml

# === Python (Fabric / Lambdas) ===
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# fab env.up failure logs (auto-generated, local only)
.fab-logs/
